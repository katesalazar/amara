#   Vim stuff.
*.swp

#   KDevelop stuff.
amara.kdev4
.kdev4/
*.kate-swp

#   macOS stuff.
.DS_Store

#   Xcode stuff.
amara.xcodeproj/project.xcworkspace/
amara.xcodeproj/xcuserdata/
amara.xcodeproj/xcshareddata/

#   Bison object docs.
minia.tab.h
minia.tab.c

#   Bison 2.3 reporting stuff (macOS High Sierra - no Homebrew use).
src/bsn/minia.vcg
src/bsn/minia.output

#   Bison 3.1 reporting stuff.
src/bsn/minia.report
src/bsn/minia.dot

# TODO    Document this. */
build/src/bsn/minia.l
build/src/bsn/minia.y
build/src/bsn/minia.dot
build/src/bsn/minia.output

#   Flex object docs.
build/src/bsn/lex.minia.c
# TODO     Make it so this doc appear only in `BASE/build/src/` and
# TODO   never in `BASE/src/`.
src/bsn/lex.minia.c

#   Bison object docs.
build/src/bsn/minia.tab.h
build/src/bsn/minia.tab.c
# TODO     Make it so these docs appear only in `BASE/build/src/` and
# TODO   never in `BASE/src/`.
src/bsn/minia.tab.h
src/bsn/minia.tab.c

#   Bison reporting.
src/bsn/minia.report
src/bsn/minia.dot

#   Object docs.
build/src/*.c
build/src/*.cpp
build/src/*.h
build/src/*/*.c
build/src/*/*.cpp
build/src/*/*.h
build/debug/*.o
build/release/*.o
build/release/amara_not_stripped
build/release/amara
build/debug/amara_g_not_stripped
build/debug/amara_g

#   macOS debugging symbols.
amara_g.dSYM/

#   `gprof` stuff.
gmon.out

#   GCC coverage output docs.
build/debug/*.gcno
build/release/*.gcno

#   Coverage raw data.
build/debug/*.gcda
build/release/*.gcda

#   `gcov` output docs.
*.gcov

#   `gcov` standard and error output, recorded.
gcov_stdout
gcov_stderr

#   `lcov` output docs.
lcov.info

#   `lcov`'s `genhtml` output docs.
gcov_results/

#   Miscellaneous coverage stuff. This is a symbolic link as a
# workaround for a presentation issue in `lcov`.
build/debug/minia.l

#   Miscellaneous coverage stuff. This is a symbolic link as a
# workaround for a presentation issue in `lcov`.
build/debug/minia.y

#   Doxygen stuff.
doxygen_reports/
